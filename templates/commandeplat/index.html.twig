{% extends 'base.html.twig' %}

{% block title %}Hello CommandeplatController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


    <div class="bg-light p-3">
         <h1>votre panier</h1>
    </div>

    {% if items |length >0 %}
         <table class="table">
             <thead>
             <tr>
                 <th>Titre </th>
                 <th>Prix</th>
                 <th>Quantité</th>
                 <th>Total</th>
                 <th></th>
             </tr>
             </thead>
           <tbody>
           {%  for item in items %}
               <tr>
                   <td>{{ item.plat.reference }}</td>
                   <td>{{ item.plat.prix }}</td>
                   <td>{{ item.quantity }}</td>
                   <td> {{ item.plat.prix * item.quantity }}</td>
                   <td>
                       <a href="#"class="a.btn.btn-danger.btn-sm">
                           <i class="fas fa-trash"> </i>
                       </a>
                   </td>
               </tr>
           {% endfor  %}
           </tbody>
         </table>
    {% endif %}

{% endblock %}
