
{% block body %}
 <h5>la fourchette </h5>

                    <!-- /.card-header -->
                    <div class="card-body table-responsive p-0">
                        <div class="w3-serif">

                <h1 align="center" style="color:#fa521c" > Liste des Evenements </h1>
                        </div>
                        <table class="table table-hover text-nowrap" border="1">
                            <thead>
                            <tr>
                                <th>Id</th>
                                <th>Designation</th>
                                <th>Description</th>
                                <th>Date</th>
                                <th>Image</th>
                                <th>Nombre de Participants</th>

                            </tr>
                            </thead>
                            <tbody>
                            {% for c in evenements %}
                                <tr>
                                    <td>{{c.idE}}</td>
                                    <td>{{c.designationE}}</td>
                                    <td>{{c.descriptionE}}</td>
                                    <td>{{c.dateE | date}}</td>
                                    <td><img src="{{asset('uploads/images/')}}{{c.imageE}}" alt="{{c.imageE}}" height="52" width="52"></td>
                                    <td>{{c.nbrParticipants}}</td>

                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
    </div>
    </section>
{% endblock %}
