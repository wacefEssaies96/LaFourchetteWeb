{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
<div class="content-wrapper">
<section class="content">
<div class="container-fluid">
<div class="card">
              <div class="card-header">
                <h3 class="card-title">List des Utilisateurs</h3>

                <div class="card-tools">
                  <div class="input-group input-group-sm" style="width: 150px;">
                    <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                    <div class="input-group-append">
                      <button type="submit" class="btn btn-default">
                        <i class="fas fa-search"></i>
                      </button>
                    </div>
                  </div>
                </div>
              </div>
              <!-- /.card-header -->
              <div class="card-body table-responsive p-0">
                <table class="table table-hover text-nowrap">
                  <thead>
                    <tr>
                      <th>Id</th>
                      <th>Email</th>
                       <th>Rôle</th>
                       <th>Etat</th>
                        <th>image</th>
                        <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                  {% for p in utilisateurs %}
                    <tr>
                      <td>{{p.id}}</td>
                      <td>{{p.email}}</td>
                     <td>{% for  role in p.roles  %}
                    {{ role }}
                     {% endfor %}                
                       </td>
                   <td>{{p.verif}}</td>
                      <td><img src="{{asset('uploads/images/')}}{{p.picture}}" width="52" height="52" > </img></td>


                       <td>
                        <a href="{{ path('deleteUtilisateur',{'id':p.id}) }}">Delete</a>
                        <a href="{{ path('updateUtilisateur',{'id':p.id}) }}">Update</a>
                        <a href="{{ path('blocuser',{'id':p.email}) }}">Bloque</a>
                        <a href="{{ path('deblocuser',{'id':p.email}) }}">Debloque</a>

                      </td>
                    </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
            </div>
            </div>
            </section>
{% endblock %}
