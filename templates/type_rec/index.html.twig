{% extends 'Back/Back-base.html.twig' %}

{% block title %}Type réclamation{% endblock %}

{% block body %}
<div class="content-wrapper">
<section class="content">
<div class="container-fluid">
<div class="card">
              <div class="card-header">
                <h3 class="card-title">List des types des réclamations</h3>

                <div class="card-tools">
                  <div class="input-group input-group-sm" style="width: 150px;">
                    <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                    <div class="input-group-append">
                      <button type="submit" class="btn btn-default">
                        <i class="fas fa-search"></i>
                      </button>
                    </div>
                  </div>
                </div>
              </div>
              <!-- /.card-header -->
              <div class="card-body table-responsive p-0">
             
                <table class="table table-hover text-nowrap">
                  <thead>
                    <tr>
                      <th>Type de réclamation</th>
                      <th>Réference</th>
                      <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                  {% for p in typeRecs %}
                    <tr>
                      <td>{{p.typeRec}}</td>
                      <td>{{p.refT}}</td>
                      <td>
                        <a href="{{ path('deleteTypeRec',{'id':p.typeRec}) }}">Delete</a>
                        <a href="{{ path('updateTypeRec',{'id':p.typeRec}) }}">Update</a>
                      </td>
                    </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
            </div>
            </div>
            </section>
{% endblock %}
